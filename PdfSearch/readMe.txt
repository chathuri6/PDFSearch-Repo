index.php => redirects user to login page(index_sub.php)
searchResultFilesList.php => list down file names result
searchResultPagesList.php => list down pages and content result for specific file names

In listing page and containing sentences
commented if searched string is a sentence because that is automatically done through if half the word condition.

if(one word)
{
	//searched by one exact word
}
else
{
	//searched by half the word or a sentences
}


